{
  "name": "numberdepot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "verify:payments": "node scripts/verify-payment-flow.mjs",
    "migrate:mongo": "node scripts/migrate-mongo.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
